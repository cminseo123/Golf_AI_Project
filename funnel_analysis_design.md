# 데이터 분석 퍼널 설계도 (The Golf Code)

> 작성: 2026-06-23 / 최종 갱신: 2026-08-01 / 기준 코드: `index.html`, `golf-*.html` (G-XV498H772W)
> 목적: "유형 분류 → 상품 연결" 퍼널의 각 단계를 측정 가능한 GA4 이벤트로 매핑하고,
> 어떤 수치가 어떤 의사결정을 촉발하는지 정의한다.

---

## 0. 실제 구현된 이벤트 (코드 검증 완료)

| 이벤트 | 발화 시점 | 핵심 파라미터 | index.html |
|------|------|------|------|
| `test_start` | 시작 버튼 클릭 | `event_label`(골퍼모드/일반모드) | L1329 |
| `answer_question` | 문항 응답마다 | `question_number` | L1084 |
| `result_type` | **결과 계산 즉시** | `type_ko/en/key`, `play_mode` | L1119 |
| `result_view` | **2초 로딩 생존 후** | `type_ko/en/key`, `play_mode` | L1134 |
| `result_detail_click` | 유형 상세 페이지 이동 | `type_ko` | L1630 |
| `article_click` | 추천 기사 클릭 | `article_href`, `type_ko` | L1609 |
| `swing_analysis_click` | AI 스윙분석 배너/카드 | `source`(banner/card), `type_ko` | L1397,1700 |
| `result_share` | 결과 공유 | `channel`(native/twitter/facebook/threads/copy_link), `type_ko` | L1159~1198 |
| `email_signup` | 베타 이메일 등록 (**전송 성공 시에만**) | `golf_level`, `type_ko` | L1022 |
| `article_cta_click` | **정적 기사 → 테스트 이동** (2026-07-28 추가) | `cta_position`(mid/bottom/nav), `article_slug`, `cta_variant`(a/b/c, 2026-08-01 추가) | `golf-*.html` 각 파일 하단 인라인 스크립트 |

⚠️ GA4 자동 수집: `page_view`, `scroll`, `session_start`, `first_visit`.

> **계측 공백 해소 (2026-07-28)**
> 그동안 정적 기사 페이지(`golf-*.html` 20개)에는 gtag 기본 스니펫만 있고 **커스텀 이벤트가 0건**이었다. 즉 기사 독자가 테스트로 넘어왔는지를 측정할 방법이 아예 없었음. `article_cta_click`으로 해소.
> - `mid` = 기사 본문 1절 직후에 새로 삽입한 CTA (본문 9~24% 지점, 평균 15%)
> - `bottom` = 기존 하단 CTA (본문 100% 지점) — 기존에도 있었으나 미계측
> - `nav` = 헤더 내비 "테스트" 링크
> **mid vs bottom 클릭 비교가 이 이벤트의 핵심 목적.** 기존 bottom CTA는 사실상 전환을 못 만들고 있었으므로(아래 5.5절), 더 이른 위치가 실제로 먹히는지 판정한다.

> **데이터 품질 노트 (2026-07-21 갱신)**
> - `email_signup`은 Formspree 저장이 **실제 성공했을 때만** 발사된다(2026-07-21 수정). 실패 시엔 화면에 에러를 표시하고 이벤트를 보내지 않는다. 이전에는 실패해도 성공으로 위장돼 유실이 은폐됐음.
> - ⚠️ **Formspree 무료 플랜은 월 50건 한도.** 한도 초과 시점부터 신청이 실패 → `email_signup`(아래 G3 이메일 전환율)이 **실제 신청보다 적게 잡힐 수 있음.** 월말/유입 급증 시 지표 하락은 관심 저하가 아니라 한도 소진일 가능성을 먼저 의심할 것. → 실사용 늘면 유료 전환 또는 자체 수집 엔드포인트(Cloudflare Pages Functions) 이전 검토.

---

## 1. 매크로 전환 퍼널 (전체 흐름)

```
[A] page_view            방문                          100%   ← 분모
        │
[B] scroll (90%)         소개 읽음 / 관심                      ↓ 이탈①
        │
[C] test_start           테스트 시작                          ↓ 이탈② (★최대 이탈, 현재 ~38%)
        │
[D] answer_question(1)   첫 문항 응답                         ↓ 이탈③ (시작했으나 첫 답 안 함)
        │
[E] result_type          끝까지 응답 → 계산 도달                ↓ 이탈④ (중도 포기)
        │
[F] result_view          2초 로딩 생존 → 결과 실제 노출          ↓ 이탈⑤ (★로딩 이탈, 신규 측정구간)
        │
        ├──[G1] result_share          공유 (바이럴 루프)
        ├──[G2] swing_analysis_click  AI분석 관심 (지불의향 신호)
        ├──[G3] email_signup          베타 전환 (리드)
        ├──[G4] article_click         콘텐츠 소비 (재방문 가치)
        └──[G5] result_detail_click   유형 탐색 깊이
```

### 단계별 핵심 비율 (KPI)

| # | 비율 | 계산식 | 의미 | 현재값(추정) | 목표 |
|---|------|------|------|------|------|
| 이탈② | **시작율** | C ÷ A | 랜딩 설득력 | 38% | 50% |
| 이탈④ | **완료율** | E ÷ C | 테스트 흡입력 | 76% | 80%+ |
| 이탈⑤ | **로딩 생존율** | F ÷ E | 2초 애니 이탈 | **미측정** | 측정 시작 |
| G2 | **AI분석 관심율** | G2 ÷ F | 지불의향 | 미측정 | 세그먼트별 비교 |
| G3 | **이메일 전환율** | G3 ÷ F | 리드 획득 | 미측정 | 세그먼트별 비교 |
| G1 | **바이럴 계수** | G1 ÷ F | 자가 트래픽 생성 | 미측정 | >0.1 |

> ★ `result_type` vs `result_view` 갭(이탈⑤)이 크면 2초 로딩 애니메이션이 손해.
> 갭이 10%↑면 로딩 단축/스킵 버튼 검토.

---

## 2. 세그먼트 축 (이 프로젝트의 핵심 자산)

퍼널을 **단일 숫자가 아니라 세그먼트 교차로** 봐야 함. 거의 모든 이벤트에 `type_ko`가 붙어 있어 가능.

> ⚠️ **예외 (2026-07-21 확인): 배너發 `swing_analysis_click`(source=banner)은 `type_ko`가 항상 빈 값.** 퀴즈 진행 중(유형 계산 전)에 눌리기 때문. 따라서 아래 "유형별 지불의향" 분석은 **`source=card`(결과 페이지 카드 클릭)만** type_ko로 세그먼트 가능하고, banner 클릭은 유형별 분해 불가(전체 관심도 총량으로만 사용).

```
1차 축: type_key (6유형 / 실측 분포)        → "누가 오는가"  ★ type_ko 아님, 아래 경고 참고
2차 축: play_mode (골퍼 vs 일반인)          → "골퍼 비중 = 상품화 가능성"
3차 축: golf_level (email_signup에만 존재)  → "실력대별 전환 = 타깃 페르소나"
```

> ⚠️ **분포 분석은 `type_ko`가 아니라 `type_key`로 할 것 (2026-07-28 확정)**
> 유형은 16개가 아니라 **6개**다. 축 4개(`risk`/`mental`/`tech`/`social`) 중 상위 2개 조합이므로 경우의 수는 6가지인데, `RESULT_TYPES`에는 **5개만 정의**돼 있다. 누락된 **`mental_social`**은 DEFAULT(올라운더)로 폴백된다.
> 따라서 `type_ko`의 "올라운더"에는 **진짜 올라운더 + mental_social 폴백**이 섞인다. `type_key`로 보면 둘이 분리된다.
> `mental_social`은 2026-07-28 기준 유형 보유자 37명 중 **7명(18.9%)** — 무시할 크기가 아니다. 정식 유형 신설은 별도 과제(콘텐츠 8개 국어 + 이미지 + 상세 페이지).

### 핵심 교차 분석 4종

| 분석명 | 교차 | 답하는 질문 |
|------|------|------|
| **유형 분포** | result_type × type_ko | "어떤 유형이 가장 많은가" (첫 실측 자산) |
| **유형별 지불의향** | swing_analysis_click ÷ result_view, by type_ko | "AI형 45% vs 전략형 5%? → 첫 타깃 세그먼트" |
| **유형별 바이럴** | result_share ÷ result_view, by type_ko | "어떤 유형이 트래픽을 스스로 만드는가" |
| **전환 페르소나** | email_signup × type_ko × golf_level | "첫 유료 고객 프로필 = 유형+실력 조합" |

---

## 3. GA4 탐색(Explore) 구성 레시피

### 레시피 A — 매크로 퍼널 (이탈 지점 시각화)
```
탐색 → 유형: '유입경로 탐색 분석(Funnel exploration)'
단계: page_view → test_start → result_type → result_view → (분기) email_signup
   세분화(Breakdown): play_mode
   → 단계별 이탈%와 골퍼/일반인 차이 동시 확인
```

### 레시피 B — 유형 분포 (핵심 자산)
```
탐색 → 자유 형식(Free form)
행: type_key (맞춤 측정기준)   ← type_ko 아님! 2절 경고 참고
값: 활성 사용자                ← 이벤트 수 아님! 완주자 1명이 result_type+result_view로 2건씩 발사함
필터: event_name = result_view
   → result_type 아니라 result_view 사용 (로딩 생존자 = 진짜 결과 본 사람)
```

**2026-07-28 첫 실측** (6/18~7/28, 유형 보유자 37명, `type_ko` 기준이라 mental_social이 즐거운 긍정왕으로 표기됨):

| 유형 | 사용자 | 비중 |
|---|---|---|
| 골프 AI (`mental_tech`) | 10 | 27.0% |
| 전략적 승부사 (`mental_risk`) | 8 | 21.6% |
| **(폴백) `mental_social`** | 7 | 18.9% |
| 핵인싸 장타러 (`risk_social`) | 7 | 18.9% |
| 젠틀맨 골퍼 (`social_tech`) | 3 | 8.1% |
| 올라운더 (DEFAULT) | 2 | 5.4% |
| **와일드 테크니션 (`risk_tech`)** | **0** | **0%** |

- 상위 2개가 48.6%. 고르게 퍼진 게 아니라 **분석 성향(mental) 쪽으로 쏠려 있다** — `mental`이 포함된 조합이 상위 3개 중 3개.
- **`risk_tech` 0명은 버그가 아니라 문항 설계 결과.** RISK를 올리는 선택지가 대체로 TECH를 깎는다(예: Q3 2번 = TECH −3 / RISK +1). 두 축이 구조적으로 함께 높기 어려워 이 유형은 사실상 도달 불가에 가깝다. **문항 재설계 없이는 와일드 테크니션은 계속 0에 머문다.**

### 레시피 C — 유형별 지불의향 (지불의향 매트릭스)
```
탐색 → 자유 형식
행: type_ko
열: event_name (result_view, swing_analysis_click)
   → 비율 컬럼 수기 계산 or 계산된 측정항목
```

### 레시피 D — 기사 → 테스트 교차 (2026-07-28 추가, 현재 최우선)
```
탐색 → 자유 형식
행: article_slug (맞춤 측정기준)
열: cta_position (mid / bottom / nav)
값: 이벤트 수 (필터: event_name = article_cta_click)
   → ① mid가 bottom보다 유의하게 많은가 = 이른 CTA 가설 검증
   → ② 기사별 교차율 = article_cta_click ÷ 해당 기사 page_view
   → ③ 웨지 가이드(최대 유입)의 교차율이 판정 기준선
```

### 사전 준비: 맞춤 측정기준 등록 (✅ 3개 전부 완료)

`article_cta_click`의 세 파라미터 전부 **이벤트 범위**로 등록 완료. 레시피 D·E 판정 가능 상태.

| 매개변수 | 측정기준 이름 | 등록 | 설명 |
|---|---|---|---|
| `cta_position` | 기사 CTA 위치 | ✅ | 기사에서 클릭된 테스트 CTA의 위치. mid=본문 1절 직후(약 15% 지점), bottom=본문 최하단, nav=상단 내비 테스트 링크. mid vs bottom 비교가 목적. |
| `article_slug` | 기사 슬러그 | ✅ | CTA가 클릭된 기사 파일명(.html 제외). 예: golf-wedge-guide. 기사별 교차율(article_cta_click ÷ 해당 기사 page_view) 산출에 사용. |
| `cta_variant` | 문구 유형 CTA | ✅ 8/1 | 검색의도 클러스터별 CTA 문구. a=기준·비교형(6편), b=기술 노하우형(10편), c=성향·멘탈형(4편). 2026-08-01 배포분부터 수집. |

> **데이터 가용 시작 = 2026-08-01 배포 시점.** 측정기준은 소급되지 않으므로 이전 `article_cta_click`은 variant 분해가 안 된다(그 이전엔 파라미터 자체가 없었음).

> 💡 **설명란은 GA4의 순수 메모 필드**라 수집·리포트에 영향이 없다. 이미 등록된 2개는 설명을 안 채웠어도 고칠 필요 없음. 위 문구는 나중에 헷갈리지 않기 위한 참고용.
> 💡 `cta_variant`는 **드롭다운에 안 뜰 가능성이 높다** — 아직 이벤트가 발생 전이라 GA4가 매개변수를 못 받았기 때문. 6/24 `channel`·`golf_level` 때와 동일하게 **직접 타이핑**으로 등록.
> ⚠️ 측정기준 이름·설명은 사후 수정되지만 **이벤트 매개변수 이름은 등록 후 변경 불가**. 기존 2개가 `cta_position`/`article_slug`로 정확히(대소문자·언더스코어) 들어갔는지, 범위가 '이벤트'인지 한 번 확인할 것.
> 이벤트 범위 한도는 속성당 50개. 기존 11 + 신규 1 = 12개로 여유 있음.

### 기존 등록 현황 (✅ 완료 — 2026-06-24 기준 9개)

GA4 관리 → 맞춤 정의 → 이벤트 범위로 등록. **등록 완료**, 단 등록일 이후 데이터만 채워짐(소급 불가):

| 측정기준 | 등록일 | 데이터 가용 |
|------|------|------|
| `type_ko`, `play_mode`, `type_key` | 6/18 | **약 6일치 누적** ✅ 즉시 분석 가능 |
| `source`, `type_en`, `article_href`, `question_number` | 6/23 | 이틀치 (적음) |
| `channel`, `golf_level` | 6/24 | 오늘부터 (거의 빔) |

> 💡 `channel`/`golf_level`은 드롭다운에 안 떴음 → 해당 이벤트(공유·이메일) 빈도가 낮아 GA4가 매개변수를 아직 못 받았기 때문. **직접 타이핑**으로 등록 완료.

---

## 4. 의사결정 임계값 (수치 → 액션)

| 관측 | 임계 | 액션 |
|------|------|------|
| **메인 도달율** (메인 사용자÷전체 사용자) | < 50% | **기사→테스트 동선 강화** (2026-07 실측 38.8%) |
| **mid CTA 클릭수 vs bottom** | mid ≤ bottom | 이른 위치 가설 기각 → 카피/디자인 쪽으로 선회 |
| **기사 교차율** (article_cta_click÷기사 page_view) | < 3% | CTA 위치·문구 재설계, 유형별 후킹 문구 실험 |
| **네이버 기사 CTR** (클릭÷노출, 서치어드바이저) | < 2% & 노출 500↑ | 제목·설명을 고CTR 키워드 명시형으로 교체 (노출은 있는데 안 눌림 = 제목 문제) |
| **variant별 교차율 격차** (a/b/c) | 최고-최저 2배↑ | 낮은 클러스터 문구를 높은 쪽 프레임으로 재작성 |
| 시작율 (test_start÷page_view) | < 40% | ⚠️ 단독 해석 금지 — 기사 유입이 분모를 부풀린다. 5.5절 참고 |
| 로딩 생존율 (result_view÷result_type) | < 90% | 2초 로딩 단축 or 스킵 버튼 |
| 골퍼모드 비중 (play_mode) | < 50% | 일반인 유입 과다 → 상품화 어려움, 골프 채널 집중 |
| swing_analysis_click 최고 유형 | 타 유형 3배↑ | 그 유형 = golf-personal-coach 첫 타깃 확정 |
| email_signup golf_level 편중 | 특정 실력대 집중 | 첫 유료 상품을 그 실력대向 설계 |
| result_share/view | < 0.05 | 공유 인센티브/유형별 공유문구 강화 |
| 재방문율 (GA 사용자 유지) | ~0% 지속 | 앱설치·콘텐츠 알림 등 리텐션 장치 필요 |

---

## 4.5. 진행 현황 / 다음 액션 (2026-07-28 업데이트)

**완료**
- 퍼널 설계도 작성 (본 문서)
- GA4 맞춤 측정기준 9개 전부 등록 (위 3번 표 참고)
- 코드 이벤트 구현 검증 (`index.html`, 9개 이벤트 정상)
- 2026-07 실측 → 병목 규명 (5.5절): **도달율 38.8%가 진짜 병목**
- 기사 20개에 mid CTA 삽입 + `article_cta_click` 계측 (2026-07-28)
- **유입 키워드 첫 수집** (2026-08-01, 네이버 서치어드바이저) → 5.6절
- **CTA 문구 검색의도 클러스터 3종 분기 + `cta_variant` 계측** (2026-08-01) → 5.6절
- 드라이버 기사 제목·설명·h1 교체 (CTR 0.9% 개선 목적)

**⬜ 회원님 액션 필요**
1. ~~맞춤 측정기준 `cta_position`, `article_slug`, `cta_variant` 등록~~ → ✅ **3개 전부 완료 (2026-08-01)**
2. GA4 데이터 필터로 **본인 IP 제외** — 미설정 시 모든 지표가 계속 오염됨
3. GA4 → 데이터 스트림 → 원치 않는 리퍼러에 `aisearchindex.space`, `armetsrcu.asia` 추가

**바로 가능 (데이터 있음)**
- ▶ **`type_ko` 유형 분포 첫 실측** — 레시피 B. 7월 완주자 40~50명 확보되어 표본 생김
- ▶ `play_mode` 골퍼 vs 일반인 비중

**배포 후 1~2주 뒤 판정**
- ▶ **레시피 D (mid vs bottom CTA)** — 기사→테스트 교차율이 실제로 오르는지
- 유형별 지불의향(swing_analysis_click), 바이럴(channel), 전환 페르소나(golf_level)
- 권장 표본: 유형별 n≥30 (현재 하루 ~6.6명 → 이전보다 빨라짐)

---

## 5. 데이터 공백 → 채워야 할 것 (우선순위)

1. ~~맞춤 측정기준 미등록 여부 확인~~ → ✅ 완료 (2026-06-24, 9개 등록)
2. `result_type`/`result_view` 데이터 누적량 — 6/10 심은 뒤 표본 충분한지 (n≥100 권장)
3. ~~검색 키워드 미수집~~ → ✅ 해소 (2026-08-01, 네이버 서치어드바이저에서 확보. 5.6절)
   → 단, **GA4와 자동 결합은 여전히 불가.** 서치어드바이저는 별도 콘솔이라 URL 기준 수동 조인만 됨.
   → 구글(유입 9%)은 Search Console 미등록 상태 — 사이트에 `google-site-verification` 태그 없음. 우선순위 낮음.
4. 중기: 결제 퍼널 이벤트 (`upload_video → analysis_complete → payment_click → payment_success`)
   → golf-personal-coach 연결 시 추가, 본 퍼널의 G2(swing_analysis_click) 뒤에 접속

---

## 5.5. 2026-07 실측 — 병목은 "시작율"이 아니라 "도달율"이었다

기간 6/30~7/28, 활성 사용자 196명(역대 최고, 전월 110명). GA4 인기 페이지 + 소스 보고서 기준.

### 발견 1 — 테스트는 잘 작동한다. 문제는 그 앞단이다

페이지별 **사용자당 이벤트**로 분해하면 명확하다. GA4 자동수집만 발생하면 4~5, 테스트를 완주하면 커스텀 11개(`test_start`+`answer_question`×8+`result_type`+`result_view`)가 더 붙는다.

| 페이지 | 사용자 | 이벤트 | 이벤트/사용자 | 해석 |
|---|---|---|---|---|
| 메인(테스트) | 76 | 915 | **12.0** | 커스텀 다량 → 도달자의 약 68%가 완주 |
| 웨지 가이드 | 37 | 171 | 4.6 | **자동수집 바닥값 = 커스텀 0** |
| 드라이버 | 24 | 110 | 4.6 | 〃 |
| 멘탈 / 유형별연습 / 기사목록 / 에티켓 | 8/9/9/7 | 38/39/34/26 | 3.7~4.8 | 〃 |

→ **전체 196명 중 메인 페이지 도달은 76명(38.8%).** 나머지 61%는 기사만 읽고 이탈했고, 기사 페이지에서 커스텀 이벤트를 단 한 건도 발사하지 않았다.
→ 설계도 4절의 "시작율 < 40%면 랜딩 CTA 재설계" 임계값은 **오진을 부를 수 있다.** 메인 도달자는 잘 전환된다. 재설계 대상은 랜딩이 아니라 **기사→테스트 동선**.
→ 하단 CTA(본문 100% 지점)는 이전부터 있었지만 이 교차를 만들지 못했다. 그래서 mid CTA 삽입 + 계측(0절)을 실행.

### 발견 2 — 유입의 60%가 네이버, 구글은 9%

| 채널 | 사용자 | 비중 |
|---|---|---|
| **네이버 합산** (`m.search.naver.com/referral` 86 + `naver/organic` 30) | **116** | **60.1%** |
| (direct) | 44 | 22.8% |
| google / organic | 17 | 8.8% |
| 소셜 (instagram 6 + facebook 5) | 11 | 5.7% |
| bing | 5 | 2.6% |

- `m.search.naver.com/referral`과 `naver/organic`이 갈라진 것은 GA4가 네이버 **모바일** 검색을 organic으로 인식하지 못해서다. 실제로는 한 채널이며, 그중 74%가 모바일.
- **웨지 가이드 한 편이 37명**을 끌어온다(2위 기사의 1.5배). 6/5 작성분이 네이버에 잡혔다. "장비 선택" 주제가 네이버에서 먹힌다는 신호.
- 전략 함의: AdSense·구글 SEO에 들인 노력 대비 구글 유입은 9%뿐. **네이버 모바일이 실질 성장 엔진.**

### 발견 3 — 리텐션은 6개월째 0에 수렴

세션 211건 ÷ 사용자 196명 = **1.08**. 재방문 사용자 절대수는 2~7월 내내 1~7명이고, 7월엔 트래픽이 2배로 늘었는데 오히려 5명으로 줄었다. "콘텐츠 허브 = 재방문 가치" 포지셔닝은 아직 데이터로 확인되지 않는다.

### ⚠️ 이 수치를 읽을 때의 오염 요인

- **본인 IP 제외 필터가 아직 없다.** 7/21 세션에서 테스트 퍼널 1회분이 GA에 유입된 것이 확인됐고, 5월(사용자당 이벤트 29.9 / 참여 2분21초)은 TWA 빌드·QA가 집중된 달이라 본인 트래픽 비중이 컸을 가능성이 높다.
- **스팸 리퍼러 필터 미설정** (`aisearchindex.space`, `armetsrcu.asia`). 2월 지표(87명 / 참여 18초)는 베이스라인에서 제외할 것.
- GA4 "3.2천/1.2천" 표기는 반올림. 추세 판단에만 사용.

---

## 5.6. 2026-08-01 실측 — 유입 키워드 첫 수집 (네이버 서치어드바이저)

출처: 웹마스터도구 → 리포트 → 콘텐츠 노출/클릭, 최근 30일(업데이트 2026.07.31), PC+Mobile.
총 클릭 130 / 노출 5.7천 / 평균 CTR 2.2% (전월 대비 클릭 +126.3%, 노출 +40.5%).

### 발견 1 — 기사 유입은 사실상 100% 네이버 검색

서치어드바이저 클릭과 GA4 사용자 수가 거의 1:1로 붙는다.

| 기사 | 네이버 클릭(7월) | GA 사용자(6/30~7/28) |
|---|---|---|
| 웨지 가이드 | 40 | 37 |
| 드라이버 | 26 | 24 |
| 멘탈 | 9 | 8 |
| 에티켓 | 5 | 7 |

→ 기사 페이지에는 구글·직접·소셜 유입이 거의 없다. **기사 트래픽 = 네이버 검색 단일 채널.**
→ 5.5절 발견1(기사 페이지 이벤트/사용자 4.6 = 커스텀 0건)과 겹치면: **네이버 검색 클릭 약 90건이 테스트로 단 한 명도 넘어가지 않았다.**

### 발견 2 — "차이·비교"형은 먹히고 "노하우"형은 노출만 먹는다

| 의도 유형 | 키워드 예 | 노출 | CTR |
|---|---|---|---|
| 비교·차이형 | `웨지 차이` / `aw sw 차이` / `드라이버 방향성` | 13~32 | **13.3~15.6%** |
| 스펙 질문형 | `58웨지와 pw차이` / `웨지 pw aw sw 차이` | 18~43 | 7~11.1% |
| 노하우형 | **`드라이버 잘치는법`** | **1,695** | **0.4%** |

- `드라이버 잘치는법` 하나가 전체 노출의 30%(1,695)인데 클릭은 6건. 네이버 통합검색에서 이 질의는 블로그·동영상이 상위를 점유해 웹문서는 하위 노출된다.
- 웨지 가이드는 노출이 절반(1,227)인데 클릭은 6.7배(40). **노출량이 아니라 질의 유형이 클릭을 만든다.**
- **전략 함의: 신규 콘텐츠는 노하우형이 아니라 비교·스펙형으로.** (예: "아이언 5번 vs 7번", "로프트 9.5도 10.5도 차이", "골프공 2피스 3피스") 이 사이트가 네이버에서 이기는 유일한 포맷.

### 발견 3 — 드라이버 기사 CTR이 최대 단일 레버

노출 2,756(1위)에 클릭 26 = CTR 0.9%. 웨지 수준(3.3%)까지만 올려도 클릭 91건 → **전체 유입 130 → 195 (+50%).** 신규 집필보다 비용이 훨씬 싸다.
→ 2026-08-01 조치: 제목/설명/h1을 실제 고CTR 키워드(`드라이버 방향성` 15.6%, `드라이버 드로우 치는법` 4.2%)를 명시하는 방향으로 교체. `드라이버 잘치는법`은 노출 자산이므로 유지.

### 발견 4 — 외부 백링크는 velog 단일 도메인

콘텐츠를 링크한 도메인 TOP: `velog.io` 65건 (사실상 유일). 색인 32 / 수집제한 3 / 색인제외 0.
→ 수집제한 3건은 원인 미확인. 확인 필요.

### 조치 — CTA 문구를 검색 의도 클러스터로 분기 (2026-08-01)

기존엔 기사 20편 전부 CTA 문구가 **완전히 동일**했다("골프 성향에 맞는 연습법이 따로 있습니다"). 웨지 스펙을 비교하러 온 사람과 멘탈을 찾는 사람에게 같은 말을 하고 있었고, 이것이 교차율 0의 유력한 원인. 헤드라인엔 오타(`잠긐`)까지 20편 전부에 있었다.

| variant | 클러스터 | 편수 | 후킹 |
|---|---|---|---|
| `a` | 기준·비교형 (웨지, 클럽선택, 예약, 드레스코드, 핸디캡, 에티켓) | 6 | "기준은 알았는데, 그래서 나한테 맞는 건?" |
| `b` | 기술 노하우형 (드라이버, 아이언, 그립, 숏게임, 100깨기, 코스매니지먼트, 레인지vs필드, 체력, 계절별, 초보) | 10 | "이 방법이 안 먹혔다면" |
| `c` | 성향·멘탈형 (멘탈, 유형별연습, 라운드전날, 퍼팅스타일) | 4 | "내 유형을 알면 이 글이 두 배로 쓸모 있어집니다" |

`article_cta_click`에 **`cta_variant`** 파라미터 추가(a/b/c). mid·bottom·nav 세 위치 모두에 실린다.

### 레시피 E — CTA variant 판정 (배포 후 2주)
```
탐색 → 자유 형식
행: cta_variant   열: cta_position   값: 이벤트 수
필터: event_name = article_cta_click
   → ① variant별 교차율 = article_cta_click ÷ 해당 클러스터 기사 page_view
   → ② c(성향형)가 가장 높을 것으로 예상 — 그보다 a/b가 얼마나 뒤지는지가 관전 포인트
   → ③ b는 버튼 문구도 다름("내 유형에 맞는 연습법 보기") → 문구 효과 동시 검증
```

⚠️ **`cta_variant`도 맞춤 측정기준 등록 필요.** 이제 등록 대상은 `cta_position`, `article_slug`, `cta_variant` **3개**.

---

## 6. 한 장 요약

```
방문 → [시작율 38%] → 완료 → [로딩생존 ?] → 결과
                                              │
        ┌─────────────────────────────────────┤
   바이럴(share)  지불의향(swing)  리드(email)  콘텐츠(article)
        │              │              │
   세 지표를 type_ko로 쪼개는 순간 = "첫 유료 상품 + 첫 타깃 유형" 도출
```
핵심: **단일 퍼널 숫자가 아니라 `type_ko` 교차가 이 데이터의 가치.**
